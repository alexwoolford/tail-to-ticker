# Copy values into the environment or a local .env (gitignored).
# SEC sample shape: tail-to-ticker you@real-domain
# Do not use example.com. Do not use the github-paren form (Akamai 403s it).
SEC_USER_AGENT=
